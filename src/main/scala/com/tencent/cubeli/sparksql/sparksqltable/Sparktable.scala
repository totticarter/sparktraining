package com.tencent.cubeli.sparksql.sparksqltable

import org.apache.calcite.avatica.ColumnMetaData.StructType
import org.apache.spark.{SparkConf, SparkContext}
import org.apache.spark.sql.SparkSession
import org.apache.spark.sql.types.StructType

/**
  * Created by cubeli on 2017/9/20.
  */

object Sparktable {

  def main(args: Array[String]): Unit = {
//
//    val conf = new SparkConf().setMaster("local").setAppName("df create")
//    val sc = new SparkContext(conf)
//    val spark = SparkSession.builder().appName("Spark SQL basic example").config("spark.some.config.option", "some-value").getOrCreate()
//
//    val dfa = spark.createDataFrame([('a',1),('b',2)], StructType(List['key', 'value']))
  }
}
